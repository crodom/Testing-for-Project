note
	description: "Similar to 'link' but using another Eiffel object as the target, whichj you can add content to. Has the possibility to add the string to the file created with the generate feature."
	contracts: "require: object, ensure: edit/add to object"

class
	OBJECT_LINK

end
