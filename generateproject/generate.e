note
	description: "Converts a combination of the other classes into valid output either as a string or in a file"
	contracts: "require: input through other classes/features, ensure: valid output"

class
	GENERATE
end
