note
	description: "TITLE needs a string as an input and a desired number of a range for the size of the title (for example 1-5, 1 being biggest). The output should be 'title text', which means bigger-than-normal text"

class
	TITLE

end
